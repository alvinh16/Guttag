#!/usr/bin/python

# import math
# import re

ArrayLength = 20

for outer in range (1,ArrayLength):
     print ("outer = ", str(outer))
     for inner in range (outer,ArrayLength):
          print ("inner = ", str(inner))
     print ()
